export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// OAuth removido - app Cocos usa apenas login nativo
export const getLoginUrl = () => {
  // Retorna URL vazia - OAuth desativado
  return "";
};
